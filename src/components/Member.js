import "../styles/layout/Member.scss";

const Member = (props) => {
  /*   if (props.member === undefined) {
    return console.log("no misiela");
  } */ console.log("holi");
  console.log(props.member.name);

  return (
    <h2>pues va a ser que no miarma</h2>

    /*     <>
      
      <h3 className="name">{props.member.name}</h3> 
    </> */
  );
};
export default Member;
